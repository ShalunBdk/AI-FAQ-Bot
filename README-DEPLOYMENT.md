# 📚 Навигация по документации развертывания

Этот проект содержит несколько инструкций для развертывания. Выберите подходящую для вашего случая:

---

## 🚀 Выбор инструкции

### Для быстрого старта (15-20 минут)

**Файл:** [`QUICK-START.md`](QUICK-START.md)

**Когда использовать:**
- Нужно быстро развернуть веб-админку + Bitrix24 бот
- Есть базовый опыт с Docker
- Нужна краткая шпаргалка

**Что запустится:**
- ✅ Веб-админка
- ✅ Bitrix24 бот
- ❌ Telegram бот (не запускается)

---

### Для развертывания с Bitrix24 (подробная инструкция)

**Файл:** [`DEPLOY-BITRIX24.md`](DEPLOY-BITRIX24.md)

**Когда использовать:**
- Первое развертывание FAQ-бота
- Нужны подробные пояснения каждого шага
- Используете только Bitrix24 (без Telegram)
- **⚠️ Содержит полный список значений для замены на свои**

**Что запустится:**
- ✅ Веб-админка
- ✅ Bitrix24 бот
- ❌ Telegram бот (не запускается)

**Особенности:**
- Пошаговое руководство
- Регистрация бота в Bitrix24
- Настройка резервного копирования
- Автозапуск при перезагрузке сервера

---

### Для production развертывания (полное руководство)

**Файл:** [`DEPLOYMENT.md`](DEPLOYMENT.md)

**Когда использовать:**
- Нужна полная документация со всеми опциями
- Планируете запускать все сервисы (веб + Telegram + Bitrix24)
- Нужна настройка мониторинга и безопасности

**Что запустится:**
- ✅ Веб-админка
- ✅ Telegram бот (опционально)
- ✅ Bitrix24 бот (опционально)

**Особенности:**
- Полное руководство по всем сервисам
- Настройка Nginx
- Резервное копирование
- Мониторинг и алерты
- Масштабирование

---

## ⚠️ Обязательно прочитайте перед началом

### Замена значений-заполнителей

Во всех инструкциях используются общие примеры:

| Заполнитель | Замените на | Пример |
|-------------|-------------|--------|
| `your-domain.com` | Ваш публичный домен | `bot.company.com` |
| `your-company.bitrix24.ru` | Ваш портал Bitrix24 | `mycompany.bitrix24.ru` |
| `your-server` | IP/hostname сервера | `192.168.1.100` |

**📋 Полный checklist замен:** см. файл [`DEPLOY-BITRIX24.md`](DEPLOY-BITRIX24.md) → раздел "⚠️ Перед началом"

---

## 📁 Конфигурационные файлы

### Docker Compose

| Файл | Назначение |
|------|------------|
| `docker-compose.production.yml` | Production конфигурация (веб + Bitrix24 по умолчанию) |
| `docker-compose.yml` | Development конфигурация (в папке `docker/`) |

### Nginx конфигурации

| Файл | Назначение |
|------|------------|
| `nginx-bitrix-only.conf` | **Рекомендуется**: Только веб-админка + Bitrix24 |
| `nginx-complete-config.conf` | Полная конфигурация со всеми сервисами |
| `nginx-faq-config.conf` | Отдельные location блоки для добавления в существующий конфиг |

### Переменные окружения

| Файл | Назначение |
|------|------------|
| `docker.env.production` | Шаблон для production (.env) |
| `.env.example` | Шаблон для development |

---

## 🔧 Быстрые команды

### Запуск

```bash
# Веб-админка + Bitrix24 бот
docker-compose -f docker-compose.production.yml up -d

# + Telegram бот (если нужен)
docker-compose -f docker-compose.production.yml --profile telegram up -d
```

### Логи

```bash
# Все сервисы
docker-compose -f docker-compose.production.yml logs -f

# Только веб-админка
docker logs -f faqbot-web-admin

# Только Bitrix24 бот
docker logs -f faqbot-bitrix24-bot
```

### Перезапуск

```bash
docker-compose -f docker-compose.production.yml restart
```

### Остановка

```bash
docker-compose -f docker-compose.production.yml down
```

---

## 🆘 Проблемы при развертывании

### 502 Bad Gateway

```bash
# Проверьте статус контейнеров
docker-compose -f docker-compose.production.yml ps

# Проверьте сеть
docker network inspect nginx_default
```

### ChromaDB ошибки

```bash
# Проверьте права доступа
sudo chmod -R 777 data/

# Переобучите базу
docker exec -it faqbot-web-admin python -c "from src.web.web_admin import retrain_chromadb; retrain_chromadb()"
```

### Бот не отвечает

```bash
# Проверьте логи
docker logs -f faqbot-bitrix24-bot

# Проверьте переменные окружения
docker exec faqbot-bitrix24-bot env | grep BITRIX24
```

---

## 📖 Дополнительная документация

- **Архитектура проекта:** [`CLAUDE.md`](CLAUDE.md)
- **README пользователя:** [`README.md`](README.md)

---

## 🎯 Рекомендуемый путь для новичков

1. ✅ Прочитайте **DEPLOY-BITRIX24.md** → раздел "⚠️ Перед началом"
2. ✅ Замените все значения-заполнители на свои
3. ✅ Следуйте **QUICK-START.md** для быстрого развертывания
4. ✅ При проблемах обратитесь к **DEPLOYMENT.md** (раздел "Устранение проблем")

---

**Версия:** 1.0
**Дата:** 2025-01-18
