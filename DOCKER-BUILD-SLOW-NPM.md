# 🐌 Решение проблемы медленного npm install в Docker

## Проблема

При сборке Docker образа `npm install` "зависает" или выполняется очень долго (10+ минут).

**Причины:**
- Медленное интернет-соединение на сервере
- Проблемы с доступом к npm registry
- Таймауты при скачивании пакетов

---

## ✅ Решения

### Вариант 1: Использовать Dockerfile с логированием (рекомендуется)

Стандартный `docker/Dockerfile` уже обновлён - теперь показывает прогресс:

```bash
docker compose --profile bitrix24 build --no-cache
```

**Вывод будет:**
```
==> Настройка npm timeouts...
==> Установка npm зависимостей (может занять несколько минут)...
npm info it worked if it ends with ok
npm info using npm@x.x.x
...
==> Загрузка шрифтов...
==> Сборка CSS...
==> Очистка npm кэша...
==> Готово!
```

**⏱️ Ожидаемое время:** 5-15 минут в зависимости от скорости интернета

---

### Вариант 2: Собирать CSS локально (БЫСТРО!)

Соберите CSS на локальной машине и используйте `Dockerfile.no-npm`:

#### Шаг 1: Соберите CSS локально

```bash
# На локальной машине (Windows/Mac/Linux)
npm install
npm run build:css
```

**Результат:** файл `src/web/static/css/output.css` (готов к копированию в Docker)

#### Шаг 2: Используйте Dockerfile без npm

Отредактируйте `docker-compose.production.yml`:

```yaml
services:
  faqbot-web-admin:
    build:
      context: .
      dockerfile: docker/Dockerfile.no-npm  # ← Используем альтернативный Dockerfile
```

#### Шаг 3: Соберите образ

```bash
docker compose --profile bitrix24 build --no-cache
```

**⏱️ Ожидаемое время:** 2-5 минут (без npm install!)

---

### Вариант 3: Использовать npm зеркало (для корпоративных сетей)

Если у вас есть внутреннее зеркало npm registry:

```dockerfile
# В docker/Dockerfile, перед npm install:
RUN npm config set registry https://your-npm-mirror.company.com
```

---

### Вариант 4: Кэшировать node_modules (multi-stage build)

Создайте `docker/Dockerfile.cached`:

```dockerfile
# Stage 1: Сборка статических файлов
FROM node:18-alpine AS builder

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install

COPY . .
RUN npm run build:css

# Stage 2: Production образ
FROM python:3.11-slim

WORKDIR /app

# Установка Python зависимостей
COPY requirements.txt .
RUN pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
RUN pip install --no-cache-dir -r requirements.txt

# Копируем всё кроме node_modules
COPY . .

# Копируем только собранный CSS из builder stage
COPY --from=builder /app/src/web/static/css/output.css src/web/static/css/

# Создаем директории
RUN mkdir -p /app/data/chroma_db

ENV ANONYMIZED_TELEMETRY=False
EXPOSE 5000 5001 5002
CMD ["bash"]
```

---

## 🔍 Диагностика проблем

### Проверка доступности npm registry

На сервере:

```bash
# Проверка доступа к npm registry
curl -I https://registry.npmjs.org/

# Должно вернуть: HTTP/2 200
```

Если не доступен → используйте **Вариант 2** или **Вариант 3**.

### Проверка скорости интернета

```bash
# Скачайте тестовый пакет
time npm install lodash --registry https://registry.npmjs.org/
```

Если > 30 секунд → используйте **Вариант 2**.

### Просмотр реального прогресса

Откройте **второй терминал** и следите за логами:

```bash
# Терминал 1: Сборка
docker compose --profile bitrix24 build

# Терминал 2: Мониторинг (в другом окне)
watch -n 1 'docker ps -a | grep build'
```

---

## 📊 Сравнение вариантов

| Вариант | Время сборки | Сложность | Когда использовать |
|---------|-------------|-----------|-------------------|
| **Вариант 1** (с логами) | 5-15 мин | Низкая | Стабильный интернет |
| **Вариант 2** (локальный CSS) | 2-5 мин | Средняя | Медленный интернет на сервере |
| **Вариант 3** (npm зеркало) | 3-8 мин | Средняя | Корпоративная сеть с прокси |
| **Вариант 4** (multi-stage) | 6-12 мин | Высокая | Частые пересборки |

---

## ✅ Рекомендации

### Для production сервера с медленным интернетом:
→ **Вариант 2** (локальный CSS)

### Для CI/CD pipeline:
→ **Вариант 4** (multi-stage с кэшированием)

### Для разработки:
→ **Вариант 1** (стандартный Dockerfile)

---

## 🐛 Troubleshooting

### Проблема: npm install зависает на конкретном пакете

```bash
# Смотрим что именно скачивается
docker compose build --progress=plain 2>&1 | tee build.log
grep "npm http fetch" build.log
```

**Решение:** Добавить этот пакет в `.npmrc` с зеркалом

### Проблема: "ENOTFOUND registry.npmjs.org"

**Причина:** DNS проблемы

**Решение:**
```bash
# Добавьте в docker-compose.yml
services:
  faqbot-web-admin:
    dns:
      - 8.8.8.8
      - 8.8.4.4
```

### Проблема: "ETIMEDOUT" при npm install

**Решение:** Используйте **Вариант 2** (локальный CSS)

---

## 📝 История версий

**2025-01-26:**
- Добавлены verbose логи в Dockerfile
- Создан Dockerfile.no-npm для локальной сборки CSS
- Увеличены npm timeouts: 300s (было 60s)
- Добавлены retry параметры: 5 попыток

---

## 🔗 См. также

- [DOCKER-CPU-OPTIMIZATION.md](DOCKER-CPU-OPTIMIZATION.md) - оптимизация PyTorch
- [README_ASSETS.md](README_ASSETS.md) - сборка CSS локально
