# Временный фикс для static_url_path (если ProxyFix не работает)
# Применить только если пересборка с ProxyFix не помогла!

# В src/web/web_admin.py:

# БЫЛО (новый код с ProxyFix):
app = Flask(__name__,
            static_folder=static_folder,
            template_folder=template_folder,
            static_url_path='/static')  # БЕЗ BASE_PATH

app.wsgi_app = ProxyFix(app.wsgi_app, x_prefix=1)

# ВРЕМЕННО ВЕРНУТЬ (пока не разберётесь с ProxyFix):
app = Flask(__name__,
            static_folder=static_folder,
            template_folder=template_folder,
            static_url_path=f"{BASE_PATH}/static")  # С BASE_PATH

# И ЗАКОММЕНТИРОВАТЬ ProxyFix:
# app.wsgi_app = ProxyFix(app.wsgi_app, x_prefix=1)
