#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тестовый скрипт для проверки лемматизации ключевых слов

Проверяет, что лемматизация правильно приводит разные формы слов
к начальной форме для улучшения keyword search.
"""

import sys
import os

# Добавляем корневую директорию в PYTHONPATH
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from src.core.search import lemmatize_word, lemmatize_text, extract_keywords


def test_lemmatize_word():
    """Тест лемматизации отдельных слов"""
    print("=" * 60)
    print("ТЕСТ 1: Лемматизация отдельных слов")
    print("=" * 60)

    test_cases = [
        # Существительные (разные падежи)
        ("претензия", "претензия"),
        ("претензию", "претензия"),
        ("претензии", "претензия"),
        ("претензий", "претензия"),

        # Существительные (единственное/множественное число)
        ("жалоба", "жалоба"),
        ("жалобу", "жалоба"),
        ("жалобы", "жалоба"),

        ("нарушение", "нарушение"),
        ("нарушения", "нарушение"),
        ("нарушений", "нарушение"),

        ("товар", "товар"),
        ("товара", "товар"),
        ("товары", "товар"),

        # Прилагательные
        ("некачественный", "некачественный"),
        ("некачественного", "некачественный"),
        ("некачественные", "некачественный"),

        ("бракованный", "бракованный"),
        ("бракованного", "бракованный"),
        ("бракованные", "бракованный"),

        # Глаголы
        ("составить", "составить"),
        ("составлю", "составить"),
        ("составляю", "составлять"),

        ("повредить", "повредить"),
        ("повредил", "повредить"),
        ("повредили", "повредить"),
    ]

    passed = 0
    failed = 0

    for word, expected in test_cases:
        result = lemmatize_word(word)
        status = "✅" if result == expected else "❌"

        if result == expected:
            passed += 1
        else:
            failed += 1

        print(f"{status} {word:20} → {result:20} (ожидалось: {expected})")

    print(f"\nРезультат: {passed} пройдено, {failed} не пройдено")
    print()


def test_lemmatize_text():
    """Тест лемматизации текста"""
    print("=" * 60)
    print("ТЕСТ 2: Лемматизация текста")
    print("=" * 60)

    test_cases = [
        "Как составить претензию?",
        "У меня проблемы с оборудованием",
        "Я хочу вернуть бракованный товар",
        "Нужна справка о доходах",
        "Повредили товар при доставке",
        "Требование о возврате денег",
    ]

    for text in test_cases:
        lemmas = lemmatize_text(text)
        print(f"Текст:  {text}")
        print(f"Леммы: {lemmas}")
        print()


def test_extract_keywords():
    """Тест извлечения ключевых слов с/без лемматизации"""
    print("=" * 60)
    print("ТЕСТ 3: Извлечение ключевых слов")
    print("=" * 60)

    test_cases = [
        "Как составить претензию по некачественному товару?",
        "У меня проблемы с оборудованием и нужна помощь",
        "Хочу подать жалобу на поставщика",
        "Требование о возврате денег за бракованный товар",
    ]

    for text in test_cases:
        print(f"Запрос: {text}")
        print()

        # Без лемматизации
        keywords_no_lemma = extract_keywords(text, use_lemmatization=False)
        print(f"  БЕЗ лемматизации: {keywords_no_lemma}")

        # С лемматизацией
        keywords_with_lemma = extract_keywords(text, use_lemmatization=True)
        print(f"  С лемматизацией:  {keywords_with_lemma}")
        print()


def test_keyword_matching():
    """Тест сопоставления ключевых слов в разных формах"""
    print("=" * 60)
    print("ТЕСТ 4: Сопоставление ключевых слов")
    print("=" * 60)

    # Ключевые слова из FAQ (начальные формы)
    faq_keywords_raw = "претензия, жалоба, требование, нарушение, рекламация, составить, некачественный, повредить, оборудование, повреждение, бой, недостача, товар, брак, бракованный, перевыставление, штраф, ответственность, возврат"
    faq_keywords = extract_keywords(faq_keywords_raw, use_lemmatization=True)

    print(f"Ключевые слова FAQ (лемматизированные):")
    print(f"  {faq_keywords}")
    print()

    # Запросы пользователя в разных формах
    user_queries = [
        "Как составить претензию?",
        "Мне нужно подать жалобу",
        "У меня повредили товар",
        "Хочу вернуть бракованное оборудование",
        "Требование о возврате денег",
    ]

    for query in user_queries:
        query_keywords = extract_keywords(query, use_lemmatization=True)

        # Находим совпадения
        matches = [kw for kw in query_keywords if kw in faq_keywords]
        match_count = len(matches)

        print(f"Запрос: {query}")
        print(f"  Ключевые слова: {query_keywords}")
        print(f"  Совпадений с FAQ: {match_count}/{len(query_keywords)} → {matches}")
        print()


if __name__ == "__main__":
    print("\n🔬 ТЕСТИРОВАНИЕ ЛЕММАТИЗАЦИИ КЛЮЧЕВЫХ СЛОВ\n")

    # Проверяем, установлен ли pymorphy3
    try:
        import pymorphy3
        print("✅ pymorphy3 установлен\n")
    except ImportError:
        print("❌ pymorphy3 не установлен!")
        print("   Установите: pip install pymorphy3 pymorphy3-dicts-ru\n")
        sys.exit(1)

    # Запускаем тесты
    test_lemmatize_word()
    test_lemmatize_text()
    test_extract_keywords()
    test_keyword_matching()

    print("=" * 60)
    print("✅ Все тесты завершены!")
    print("=" * 60)
