# Быстрый запуск FAQ Bot

## Что уже готово:

✅ База данных создана: `faq_database.db`
✅ 21 FAQ мигрировано из demo_faq.py
✅ Все зависимости установлены

## Запуск системы:

### 1. Веб-интерфейс (управление FAQ)

Откройте **ПЕРВЫЙ** терминал и запустите:

```bash
python web_admin.py
```

Откройте в браузере: **http://127.0.0.1:5000**

Здесь вы можете:
- Просматривать все FAQ
- Добавлять новые
- Редактировать существующие
- Удалять
- **ПЕРЕОБУЧАТЬ модель** (после любых изменений!)

### 2. Telegram бот

Откройте **ВТОРОЙ** терминал и запустите:

```bash
python bot.py
```

При первом запуске бот:
1. Загрузит модель эмбеддингов (может занять 1-2 минуты)
2. Создаст векторную базу ChromaDB из данных SQLite
3. Будет готов отвечать на вопросы в Telegram

## Workflow:

1. **Добавляете/изменяете FAQ** → в веб-интерфейсе
2. **Нажимаете "Переобучить модель"** → в веб-интерфейсе
3. **Перезапускаете бота** → Ctrl+C и снова `python bot.py`
4. **Тестируете** → в Telegram боте

## Для демонстрации:

1. Покажите веб-интерфейс на проекторе
2. Добавьте новый FAQ (например: "Где парковка?" → "Парковка находится за зданием")
3. Нажмите "Переобучить модель"
4. Перезапустите бота
5. Спросите в боте: "Где можно припарковаться?"
6. Бот найдёт ваш новый FAQ! 🎉

## Проблемы?

- Если бот не находит ответы → проверьте, что ChromaDB переобучена
- Если веб-интерфейс не открывается → проверьте, что порт 5000 свободен
- Если ошибки с эмодзи → это нормально для Windows консоли, игнорируйте

## Файлы системы:

- `faq_database.db` - SQLite база данных с FAQ
- `chroma_db/` - Векторная база ChromaDB (создаётся автоматически)
- `bot.py` - Telegram бот
- `web_admin.py` - Веб-интерфейс
- `database.py` - Модуль работы с БД
