# Changelog: Show Answer Text in Logs

## [1.0.0] - 2025-12-19

### ✨ Добавлено

**Кнопка "Показать ответ" в логах**

Теперь в колонке "Ответ" можно увидеть полный текст ответа, который получил пользователь.

#### Для обычных FAQ ответов:

**До:**
```
Вопрос FAQ
Категория
```

**После:**
```
Вопрос FAQ
Категория
[👁️ Показать ответ] ← кнопка
```

При клике раскрывается:
```
📝 Ответ пользователю: (или 🤖 RAG Ответ:)
[Полный текст ответа который увидел пользователь]
```

#### Для Clarification/No Answer:

**До:**
```
❌ Ответ не найден
```

**После:**
```
❓ Просьба уточнить (или 🚫 Информация не найдена)
[👁️ Показать текст] ← кнопка
```

При клике раскрывается:
```
🤖 RAG Ответ:
"Пожалуйста, уточните, какой именно вопрос у вас связан с письмом?..."
```

---

## Зачем это нужно?

### Проблема раньше:
- В логах показывался только **вопрос FAQ**, но не **реальный ответ**
- Для RAG особенно важно: ответ генерируется динамически, отличается от FAQ
- Невозможно проверить что именно получил пользователь
- Сложно анализировать качество RAG ответов

### Решение теперь:
- ✅ Видно реальный текст ответа (`answer_shown`)
- ✅ Для RAG показывается сгенерированный текст
- ✅ Для clarification/no_answer видно полный текст просьбы
- ✅ Удобный UI с раскрывающимися секциями

---

## Реализация

### Frontend (`logs.html`)

**Добавлена функция `toggleAnswerText()`:**
```javascript
function toggleAnswerText(answerId) {
    const answerDiv = document.getElementById(`answer-text-${answerId}`);
    if (answerDiv) {
        answerDiv.classList.toggle('hidden');
    }
}
```

**Обновлена колонка "Ответ":**

1. **Для FAQ с вопросом:**
   ```html
   <button onclick="toggleAnswerText('...')">
       <span class="material-symbols-outlined">visibility</span>
       <span>Показать ответ</span>
   </button>
   <div id="answer-text-..." class="hidden">
       <div>🤖 RAG Ответ: (или 📝 Ответ пользователю:)</div>
       <div>{{ answer_shown }}</div>
   </div>
   ```

2. **Для clarification/no_answer/none:**
   ```html
   <div>❓ Просьба уточнить (или 🚫/❌)</div>
   <button onclick="toggleAnswerText('...')">Показать текст</button>
   <div id="answer-text-..." class="hidden">
       {{ answer_shown }}
   </div>
   ```

---

## Изменённые файлы

- `src/web/templates/admin/logs.html`
  - Lines 479-484: Новая функция `toggleAnswerText()`
  - Lines 711-750: Обновлённая колонка "Ответ" с кнопками раскрытия

---

## Примеры использования

### Пример 1: RAG ответ

**Запрос:** "Как отправить письмо?"

**В логах показывается:**
```
Вопрос: Как отправить письмо?
Ответ: Отправка писем ← Вопрос FAQ
       Общая категория
       [👁️ Показать ответ]
```

**При клике на "Показать ответ":**
```
🤖 RAG Ответ:
Для отправки письма воспользуйтесь корпоративной почтой.
Зайдите в Outlook, нажмите "Создать письмо", укажите получателя
и напишите текст. Если у вас возникли проблемы, обратитесь в IT-отдел.
```

---

### Пример 2: Clarification

**Запрос:** "письмо"

**В логах показывается:**
```
Вопрос: письмо
Ответ: ❓ Просьба уточнить
       [👁️ Показать текст]
```

**При клике:**
```
🤖 RAG Ответ:
Пожалуйста, уточните, какой именно вопрос у вас связан с письмом?
Например, вас интересует отправка письма, составление письма,
проблемы с получением писем или что-то другое?
```

---

### Пример 3: No Answer

**Запрос:** "Как получить визу в Антарктиду?"

**В логах показывается:**
```
Вопрос: Как получить визу в Антарктиду?
Ответ: 🚫 Информация не найдена
       [👁️ Показать текст]
```

**При клике:**
```
🤖 RAG Ответ:
К сожалению, я не нашел информации о визе в Антарктиду.
Пожалуйста, обратитесь в отдел HR.
```

---

## Иконки и метки

| Условие | Метка | Иконка |
|---------|-------|--------|
| RAG ответ | 🤖 RAG Ответ: | 🤖 |
| Обычный ответ | 📝 Ответ пользователю: | 📝 |
| Clarification | ❓ Просьба уточнить | ❓ |
| No Answer | 🚫 Информация не найдена | 🚫 |
| None | ❌ Ответ не найден | ❌ |

---

## UI/UX Improvements

1. **Компактность**: Ответ скрыт по умолчанию, раскрывается по клику
2. **Ясность**: Метки указывают тип ответа (RAG/обычный/уточнение/не найдено)
3. **Форматирование**: `whitespace-pre-wrap` сохраняет форматирование текста
4. **Dark mode**: Полная поддержка тёмной темы
5. **Accessibility**: Material Icons для кнопок

---

## Best Practices

### Для администраторов:

1. **Проверяйте качество RAG ответов**:
   - Кликайте "Показать ответ" на записях с 🤖 RAG badge
   - Сравнивайте с оригинальным FAQ
   - Оценивайте релевантность и полноту

2. **Анализируйте clarification запросы**:
   - Смотрите какие примеры даёт RAG
   - Если часто встречается один тип запроса → создайте специфичный FAQ

3. **Мониторьте no_answer**:
   - Читайте полный текст ответа RAG
   - Проверяйте почему информация не найдена
   - Добавляйте недостающие FAQ

### Для разработчиков:

1. **Всегда логируйте `answer_shown`**:
   ```python
   database.add_answer_log(
       query_log_id=query_log_id,
       faq_id=result.faq_id,
       similarity_score=result.confidence,
       answer_shown=final_answer,  # ← Обязательно!
       search_level=result.search_level
   )
   ```

2. **Используйте `escapeHtml()`**:
   - Frontend автоматически экранирует HTML в `answer_shown`
   - Безопасно для любого текста

---

## Troubleshooting

### ❌ Кнопка "Показать ответ" не появляется

**Причина**: `answer_shown` пустой или NULL

**Решение**: Проверьте что бот логирует `answer_shown`:
```python
# В bot.py или b24_bot.py
database.add_answer_log(
    ...
    answer_shown=final_answer,  # Убедитесь что передаётся
    ...
)
```

### ❌ При клике ничего не происходит

**Причина**: JavaScript ошибка

**Решение**: Откройте консоль браузера (F12) и проверьте ошибки

### ❌ Текст не форматируется

**Причина**: Отсутствует `whitespace-pre-wrap`

**Решение**: Уже исправлено в шаблоне:
```html
<div class="whitespace-pre-wrap">{{ answer_shown }}</div>
```

---

## Breaking Changes

**Нет.** Изменения только в UI, не влияют на логику или API.

---

## Migration

**Не требуется.** Улучшение работает для всех существующих записей в `answer_logs`.

---

**Версия**: 1.0.0
**Дата**: 2025-12-19
**Статус**: ✅ Production Ready
