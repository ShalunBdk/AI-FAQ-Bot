# FAQ Bot - Руководство по установке и использованию

## Обзор системы

Система состоит из трёх основных компонентов:

1. **SQLite база данных** (`faq_database.db`) - хранит все FAQ
2. **Telegram бот** (`bot.py`) - отвечает на вопросы пользователей
3. **Веб-интерфейс** (`web_admin.py`) - управление FAQ и переобучение модели

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Мигрируйте данные из demo_faq.py в базу данных

```bash
python migrate_data.py
```

Эта команда:
- Создаст файл `faq_database.db`
- Перенесёт все FAQ из `demo_faq.py` в SQLite
- Покажет статистику по категориям

## Использование

### Запуск Telegram бота

```bash
python bot.py
```

При первом запуске бот автоматически:
- Инициализирует базу данных (если не была создана)
- Загрузит все FAQ из БД в ChromaDB для векторного поиска
- Запустится и будет готов к работе

### Запуск веб-интерфейса

```bash
python web_admin.py
```

Веб-интерфейс будет доступен по адресу: **http://127.0.0.1:5000**

## Веб-интерфейс - Возможности

### 📝 Управление FAQ

1. **Просмотр всех FAQ**
   - Все записи отображаются с категориями, вопросами, ответами и ключевыми словами
   - Можно фильтровать по категориям

2. **Добавление нового FAQ**
   - Нажмите кнопку "+ Добавить FAQ"
   - Заполните форму:
     - ID (уникальный идентификатор, например: faq_22)
     - Категория
     - Вопрос
     - Ответ
     - Ключевые слова (через запятую)
   - Нажмите "Сохранить"

3. **Редактирование FAQ**
   - Нажмите "✏️ Изменить" на нужной записи
   - Внесите изменения
   - Нажмите "Сохранить"

4. **Удаление FAQ**
   - Нажмите "🗑️ Удалить" на нужной записи
   - Подтвердите удаление

### 🔄 Переобучение векторной модели

**ВАЖНО:** После любых изменений в FAQ (добавление, редактирование, удаление) необходимо переобучить модель!

1. Нажмите кнопку **"Переобучить модель"** в веб-интерфейсе
2. Подтвердите действие
3. Дождитесь сообщения об успешном завершении

Это обновит ChromaDB с новыми данными, и бот начнёт использовать актуальную информацию.

## Структура проекта

```
FAQBot/
├── bot.py                  # Telegram бот
├── database.py             # Модуль работы с SQLite
├── web_admin.py            # Flask веб-приложение
├── migrate_data.py         # Скрипт миграции данных
├── demo_faq.py             # Исходные демо-данные (для миграции)
├── faq_database.db         # SQLite база данных (создаётся автоматически)
├── chroma_db/              # ChromaDB векторная база (создаётся автоматически)
├── templates/
│   └── index.html          # HTML шаблон веб-интерфейса
└── requirements.txt        # Зависимости Python
```

## Workflow работы с системой

### Обычный процесс:

1. **Запустите веб-интерфейс** для управления FAQ
2. **Добавьте/измените FAQ** через веб-интерфейс
3. **Нажмите "Переобучить модель"** - чтобы бот использовал новые данные
4. **Запустите бота** - он будет работать с актуальными данными

### При обновлении данных:

1. Внесите изменения в веб-интерфейсе
2. **Обязательно нажмите "Переобучить модель"**
3. Если бот уже запущен, **перезапустите его**

## API Endpoints

Веб-приложение предоставляет следующие API endpoints:

- `GET /` - Главная страница (веб-интерфейс)
- `GET /faq/list?category=HR` - Получить список FAQ (опционально по категории)
- `GET /faq/<id>` - Получить конкретный FAQ
- `POST /faq/add` - Добавить новый FAQ
- `PUT /faq/update/<id>` - Обновить FAQ
- `DELETE /faq/delete/<id>` - Удалить FAQ
- `GET /categories` - Получить список категорий
- `POST /retrain` - Переобучить ChromaDB

## Советы

1. **Регулярно делайте резервные копии** `faq_database.db`
2. **После каждого изменения** не забывайте переобучать модель
3. **Используйте понятные ID** для FAQ (например: faq_hr_1, faq_it_1)
4. **Добавляйте ключевые слова** - они улучшают качество поиска
5. **Форматируйте ответы** используя Markdown для лучшей читаемости в Telegram

## Troubleshooting

### Бот не находит ответы

- Проверьте, что в БД есть данные: запустите `migrate_data.py`
- Убедитесь, что ChromaDB переобучена
- Перезапустите бота

### Веб-интерфейс не открывается

- Проверьте, что порт 5000 не занят
- Убедитесь, что Flask установлен: `pip install Flask`

### Ошибка при переобучении

- Убедитесь, что в БД есть хотя бы одна запись FAQ
- Проверьте, что модель `paraphrase-multilingual-MiniLM-L12-v2` загружена

## Демонстрация возможностей

Для демонстрации людям:

1. Откройте веб-интерфейс на проекторе/экране
2. Покажите список FAQ с разными категориями
3. Добавьте новый FAQ в реальном времени
4. Нажмите "Переобучить модель"
5. Откройте Telegram бот и задайте вопрос по новому FAQ
6. Бот найдёт и покажет правильный ответ!

Это наглядно продемонстрирует полный цикл управления базой знаний.
